# Runs Unit Test on every commit

name: Backend Unit Tests

# on: push
on: workflow_dispatch

jobs:
  backend-unit-tests:
    uses: rropen/reusable-workflows/.github/workflows/backend_unit_tests_pytest.yml@main
